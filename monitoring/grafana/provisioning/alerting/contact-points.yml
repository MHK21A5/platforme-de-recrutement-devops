apiVersion: 1

contactPoints:
  - orgId: 1
    name: Recruitment Platform Email
    receivers:
      - uid: recruitment-platform-email
        type: email
        settings:
          # Grafana substitutes this from the container environment at startup.
          addresses: $GF_ALERT_EMAIL
